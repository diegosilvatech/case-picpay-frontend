import { Button, Container } from 'components';

import * as s from './styles';

export default function LoginPage() {
  return (
    <s.Wrapper>
      <Container>
        <Button>Login/</Button>
      </Container>
    </s.Wrapper>
  );
}
